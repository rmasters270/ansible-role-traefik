galaxy_info:
  author: rmasters270
  role_name: traefik
  description: Install Traefik Helm Chart
  company: none
  license: MIT
  min_ansible_version: '2.1'

  platforms:
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags: [kubernetes, k3s, helm, traefik, ingress, loadbalancer, reverseproxy]

dependencies: []
